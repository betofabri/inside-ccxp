// Feature flags do protótipo. Em produção (F6) migram pra env/Config no banco.

/** Aba Footprint no admin: ficha do convidado + trajeto via bipagens (dados fake até a fonte real). */
export const FOOTPRINT_ENABLED = true;
